use randomapi;
db.createUser({
    user: 'randomapi',
    pwd: 'randomapi',
    roles: [ 'readWrite', 'dbAdmin' ]
});
db.addUser({
    user: 'randomapi',
    pwd: 'randomapi',
    roles: [ 'readWrite', 'dbAdmin' ]
});
db.generators.insert({id: 1, version: '0.1'});
